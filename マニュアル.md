# 3D モデルビューアー 使用マニュアル

## 目次

1. [起動方法](#起動方法)
2. [画面構成](#画面構成)
3. [ファイルの読み込み](#ファイルの読み込み)
4. [3D 表示の操作](#3d-表示の操作)
5. [原点マーク・座標系](#原点マーク座標系)
6. [モデルの選択](#モデルの選択)
7. [モデルの平行移動](#モデルの平行移動)
8. [モデルの削除](#モデルの削除)
9. [STL エクスポート](#stl-エクスポート)
10. [表示リセット](#表示リセット)
11. [対応環境](#対応環境)

---

## 起動方法

プロジェクトのルートディレクトリで以下のコマンドを実行します。

```bash
npm start
```

---

## 画面構成

```
┌──────────────────────────────────────────────────────────────┐
│  [ファイルを開く] │ [移動*] [エクスポート] [削除*] │ [リセット]  │ ← ツールバー
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                      3D 描画エリア                            │
│                                                              │
└──────────────────────────────────────────────────────────────┘
  * モデルを選択しているときのみ有効
```

| ボタン | 状態 | 説明 |
|--------|------|------|
| ファイルを開く | 常時有効 | ファイル選択ダイアログを開く |
| 移動 | **選択時のみ有効** | 選択中のモデルを数値で移動 |
| エクスポート | 常時有効 | 全モデルを STL として書き出す |
| 削除 | **選択時のみ有効** | 選択中のモデルを削除 |
| リセット | 常時有効 | カメラを初期位置に戻す |

メニューバーの **ファイル → ファイルを開く**（`Cmd+O`）からもファイルを開けます。

---

## ファイルの読み込み

### ボタンから開く

1. ツールバーの **「ファイルを開く」** をクリック
2. ファイル選択ダイアログで `.step` / `.stp` / `.stl` ファイルを選択
3. 複数ファイルを同時に選択可能（`Cmd` または `Shift` を押しながら選択）

### ドラッグ＆ドロップ

1. ファイルを 3D 描画エリアにドラッグ
2. 「ここにファイルをドロップ」と表示されたらドロップ
3. 複数ファイルを同時にドロップ可能

> **対応形式：** `.step` / `.stp`（STEP）、`.stl`（STL）
>
> **追加読み込み：** 2 つ目以降のファイルを開いても既存のモデルは消えません。すべてのモデルが同一シーン上に表示されます。

---

## 3D 表示の操作

| 操作 | 動作 |
|------|------|
| 左ボタン ドラッグ | 視点を回転 |
| 右ボタン ドラッグ | 視点を平行移動（パン） |
| スクロール（ホイール） | ズームイン / ズームアウト |

---

## 原点マーク・座標系

3D 描画エリアの絶対座標原点（0, 0, 0）に、常時 X / Y / Z 軸の方向を示す矢印が表示されます。

本アプリは **右手系 Z-up** 座標系を採用しています（X × Y = Z、Z が鉛直上方）。

| 色 | 軸 | 方向 |
|----|----|------|
| 赤 | X | 水平（右が正） |
| 緑 | Y | 奥行き（奥が正） |
| 青 | Z | 垂直（上が正） |

---

## モデルの選択

1. 3D 描画エリアでモデルをクリック
2. 選択されたモデルが青くハイライト表示され、ツールバーに「選択中: ファイル名」と表示される
3. 同時に **「移動」** ボタンと **「削除」** ボタンが有効化される
4. 空白エリアをクリックすると選択が解除される（ボタンは再び無効化）

> STEP ファイルは複数のメッシュで構成される場合がありますが、同一ファイルのメッシュはまとめて選択されます。

---

## モデルの平行移動

選択中のモデルを X / Y / Z 方向に相対値（Δ）で移動します。

1. モデルを選択する（「移動」ボタンが有効化される）
2. ツールバーの **「移動」** をクリック
3. ダイアログに移動したい量（相対値）を入力

   | 入力欄 | 軸 | 方向 |
   |--------|-----|------|
   | X | 水平 | 右が正 |
   | Y | 奥行き | 奥が正 |
   | Z | 垂直 | 上が正 |

4. **「適用」** をクリックすると入力値がそのモデルの現在位置に加算される
5. **「キャンセル」** をクリックすると変更なしで閉じる

> **単位：** mm　**最小設定単位：** 0.0001 mm
>
> ダイアログは毎回 **0** で開きます。入力値は「今回の移動量」であり、累積して適用されます。
>
> 移動は選択中のモデルにのみ適用されます。他のモデルの位置は変わりません。

---

## モデルの削除

選択中のモデルを削除するには以下のいずれかを行います。

- ツールバーの **「削除」** ボタンをクリック
- キーボードの `Delete` キーを押す

> 削除したモデルは元に戻せません。

---

## STL エクスポート

現在シーン上に存在するすべてのモデルを、各モデルの移動量を反映した座標で 1 つの STL ファイルとして書き出します。

1. ツールバーの **「エクスポート」** をクリック
2. ファイル保存ダイアログで保存先とファイル名を指定
3. **「保存」** をクリック

> モデルが 1 つも読み込まれていない場合はエクスポートできません。
>
> 複数モデルが存在する場合、すべて 1 ファイルにまとめて出力されます。

---

## 表示リセット

ツールバーの **「リセット」** をクリックすると、カメラの位置・角度・ズームが初期状態に戻ります。

> モデルの位置（平行移動量）はリセットされません。

---

## 対応環境

| 項目 | 内容 |
|------|------|
| macOS | Apple Silicon (M1/M2/M3/M4/M5) |
| Windows | x86-64 / ARM64 |
| ランタイム | Node.js + Electron |
